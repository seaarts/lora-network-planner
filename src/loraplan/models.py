def add_one(number):
    """
    Adds 1 to `number`.

    Parameters
    ----------
    number : int
        A number to which 1 is added.

    Returns
    -------
    newNumber : int
        A new number equals to `number + 1`.
    """

    return number + 1
